// Promise: an object that represents the eventual completion (or failure) of an asynchronous operation and its resulting value.

// Readable asynchronous code.

// Promises States: Pending, Fulfilled and Rejected.

// Promise settled or resolved means a promise is either fulfilled or rejected, but no longer pending.

